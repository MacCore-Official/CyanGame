# Railway → Variables: copy these keys/values there (you don't need a local .env on Railway)
CYAN_TOKEN=your_discord_bot_token_here

# Optional: set your server (guild) ID to get instant slash-command sync for that server.
# If left blank, commands will register globally (can take up to ~1 hour).
GUILD_ID=

# Economy tuning
COOLDOWN_SECONDS=5
MIN_BET=10
MAX_BET=100000
DAILY_AMOUNT=50

# SQLite DB path (Railway ephemeral FS is fine for testing; for persistence, use a volume)
DB_PATH=cyan_economy.db
